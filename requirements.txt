# Direct dependencies - Updated for Python 3.13+ compatibility
# Note: For Python 3.11-3.12, the original versions will also work

# Core audio/speech processing
realtimestt==0.3.7
realtimetts==0.4.1
# Note: coqui-tts requires C++ build tools on Windows
# Install manually if needed: pip install coqui-tts==0.24.1

# Communication and APIs
python-socketio>=5.11.2
sseclient-py>=1.8.0
twitchAPI>=4.2.0
pyvts>=0.3.2
mss>=9.0.1

# AI/ML dependencies
chromadb>=0.5.23
transformers>=4.40.0,<4.50.0

# Fixes for dependency problems - CRITICAL for Python 3.13+
opentelemetry-proto>=1.27.0
numba>=0.60.0

# Speed up dependency resolution
contourpy>=1.2.1
umap-learn>=0.5.6
spacy[ja]>=3.7.5

# Additional explicit dependencies for stability
numpy>=1.26.0,<2.1.0
protobuf>=4.25.0,<6.0.0
